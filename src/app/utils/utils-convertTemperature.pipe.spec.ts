import { UtilsConvertTemperaturePipe } from './utils-convertTemperature.pipe';

describe('UtilsConvertTemperaturePipe', () => {
  it('create an instance', () => {
    const pipe = new UtilsConvertTemperaturePipe();
    expect(pipe).toBeTruthy();
  });
});
